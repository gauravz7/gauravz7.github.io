# ── Secrets & credentials — NEVER commit ──
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa
*_rsa
*_ed25519
*-key.json
service-account*.json
gcp-*.json
*credential*
*secret*
.aws/
.ssh/
*.tfvars

# ── Local / generated state ──
__pycache__/
.venv/
node_modules/
.DS_Store
*.log

# ── Heavy / working media (keep optimized, page-served assets tracked) ──
*.mov
*.avi
*.psd
*.tar.gz
